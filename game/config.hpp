#ifndef CONFIG_HPP
#define CONFIG_HPP

namespace game{
    namespace config{
        const short START_ROWS_COUNT {3};      // Count of checkers' rows for one
        const short START_CHECKERS_IN_ROW {4}; // Count of checkers in row at start;
    }
}

#endif // !CONFIG_HPP